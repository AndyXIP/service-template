# Copy to .env and adjust as needed. All settings have defaults, so .env is
# optional - this file just documents what's available (see src/core/config.py).
APP_NAME=service-template
ENVIRONMENT=local
LOG_LEVEL=INFO
CORS_ORIGINS=[]
DEBUG=false
